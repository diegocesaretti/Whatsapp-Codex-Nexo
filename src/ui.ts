export function renderAdminPage(): string {
  return `<!doctype html>
<html lang="es"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>WhatsApp Codex Nexo</title>
<style>
:root{font-family:Inter,system-ui,sans-serif;color-scheme:dark;--bg:#0b0d10;--panel:#14171c;--line:#2a3038;--muted:#949ca8;--good:#54d68a;--warn:#efbf52;--bad:#ff7373}*{box-sizing:border-box}body{margin:0;background:var(--bg);color:#f5f7fa}.page{width:min(1180px,calc(100% - 32px));margin:auto;padding:34px 0 80px}h1{font-size:clamp(34px,5vw,58px);letter-spacing:-.05em;margin:8px 0}.lead{color:var(--muted);max-width:820px;line-height:1.55}.grid{display:grid;grid-template-columns:repeat(12,minmax(0,1fr));gap:14px;margin-top:22px}.card{grid-column:span 6;background:var(--panel);border:1px solid var(--line);border-radius:18px;padding:18px}.wide{grid-column:1/-1}.row{display:flex;gap:10px;align-items:center;justify-content:space-between;flex-wrap:wrap}.cluster{display:flex;gap:8px;align-items:center;flex-wrap:wrap}.badge{border:1px solid var(--line);border-radius:999px;padding:5px 9px;font-size:12px;color:var(--muted)}.dot{display:inline-block;width:8px;height:8px;border-radius:50%;background:var(--warn);margin-right:6px}.dot.open{background:var(--good)}.dot.error,.dot.logged_out{background:var(--bad)}button,input,select,textarea{font:inherit}button{border:1px solid var(--line);border-radius:10px;padding:9px 12px;background:#1c2128;color:#fff;font-weight:700;cursor:pointer}button.primary{background:#f5f7fa;color:#0b0d10}button.danger{color:var(--bad);background:transparent}button:disabled{opacity:.5}input,select,textarea{width:100%;background:#0f1216;color:#fff;border:1px solid var(--line);border-radius:10px;padding:10px}textarea{min-height:110px;resize:vertical}.fields{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:10px}.fields3{display:grid;grid-template-columns:2fr 1fr auto;gap:8px;align-items:end}.field label{display:block;font-size:12px;color:var(--muted);margin:0 0 5px}.check{display:flex;gap:8px;align-items:center}.check input{width:auto}.muted{color:var(--muted)}.small{font-size:12px}.qr{background:#fff;border-radius:14px;padding:12px;width:min(340px,100%);margin-top:14px}.qr img{display:block;width:100%}.list{display:grid;gap:10px;margin-top:12px}.message{padding:10px 0;border-top:1px solid var(--line)}.message:first-child{border-top:0}.text{white-space:pre-wrap;overflow-wrap:anywhere;margin-top:4px}.status{min-height:20px;margin-top:8px}.error{color:var(--bad)}.good{color:var(--good)}.section-title{font-size:20px;font-weight:800;margin-bottom:4px}code{font-family:ui-monospace,monospace}@media(max-width:760px){.card{grid-column:1/-1}.fields,.fields3{grid-template-columns:1fr}.page{padding-top:24px}}
</style></head><body><main class="page"><div class="small muted">CODEX NEXO · WHATSAPP</div><h1>WhatsApp bridge.</h1><p class="lead">Varias cuentas de entrada observadas y una sola cuenta de salida para Codex. INPUT nunca envía; OUTPUT queda fuera del archivo general y puede habilitar un canal conversacional aislado.</p><div id="app"><div class="card wide muted">Cargando…</div></div></main>
<script>
const app=document.getElementById('app');
const esc=v=>String(v??'').replace(/[&<>'\"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#039;','\"':'&quot;'}[c]));
async function api(path,o={}){const r=await fetch(path,{cache:'no-store',...o});let b={};try{b=await r.json()}catch{}return{r,b}}
function roleLabel(r){return r==='output'?'OUTPUT · Codex':'INPUT · observado'}
function accountCard(a){const rt=a.runtime||{state:'idle'};return '<article class="card"><div class="row"><div><strong>'+esc(a.label)+'</strong><div class="small muted" style="margin-top:4px">'+roleLabel(a.role)+(a.phoneJid?' · '+esc(a.phoneJid):'')+'</div></div><span class="badge"><span class="dot '+esc(rt.state)+'"></span>'+esc(rt.state)+'</span></div>'+(rt.lastError?'<div class="error small" style="margin-top:10px">'+esc(rt.lastError)+'</div>':'')+(rt.qrDataUrl?'<div class="qr"><img src="'+esc(rt.qrDataUrl)+'" alt="QR WhatsApp"><div style="color:#111;text-align:center;font-weight:700;margin-top:6px">WhatsApp → Dispositivos vinculados</div></div>':'')+'<div class="cluster" style="margin-top:14px"><button data-connect="'+esc(a.id)+'">'+(rt.state==='open'?'Reiniciar':'Conectar / QR')+'</button><button class="danger" data-delete="'+esc(a.id)+'">Desvincular y borrar</button></div><div class="small muted" style="margin-top:10px">recibidos '+Number(rt.receivedMessages||0)+' · guardados '+Number(rt.storedMessages||0)+' · historial '+Number(rt.historyMessages||0)+'</div></article>'}
function settingsCard(s){const x=s.settings,llm=x.llm,conv=x.outputConversation||{enabled:false,authorizedNumbers:[],maxContextMessages:80},win=s.platform==='win32';return '<section class="card wide"><div class="row"><div><div class="section-title">Configuración</div><div class="small muted">Storage, Windows, comportamiento del daemon, conversación OUTPUT y resumen LLM.</div></div><span class="badge">Storage · '+esc(s.storage.mode)+'</span></div><div class="fields" style="margin-top:16px"><div class="field"><label>Backend</label><input disabled value="'+esc(s.storage.mode==='neon'?'Neon PostgreSQL · schema whatsapp_nexo':'Local JSONL')+'"></div><div class="field"><label>Datos locales / credenciales</label><input disabled value="'+esc(s.storage.dataDir)+'"></div></div><div class="fields" style="margin-top:12px"><label class="check"><input id="cfgAuto" type="checkbox" '+(x.autoConnectLinkedAccounts?'checked':'')+'> Reconectar cuentas vinculadas al iniciar</label><label class="check"><input id="cfgOpen" type="checkbox" '+(x.openDashboardOnLaunch?'checked':'')+'> Abrir panel al iniciar</label><label class="check"><input id="cfgWin" type="checkbox" '+(s.windowsAutostart?'checked':'')+' '+(win?'':'disabled')+'> Iniciar Nexo con Windows</label><div class="field"><label>Refresco UI (ms)</label><input id="cfgRefresh" type="number" min="500" max="10000" value="'+Number(x.uiRefreshMs)+'"></div><div class="field"><label>Máx. resultados búsqueda</label><input id="cfgMax" type="number" min="10" max="200" value="'+Number(x.maxSearchResults)+'"></div></div><hr style="border:0;border-top:1px solid var(--line);margin:18px 0"><div class="row"><div><strong>Conversación bidireccional con Codex</strong><div class="small muted">Sólo chats directos de números autorizados. INPUT sigue siendo no confiable.</div></div><label class="check"><input id="convEnabled" type="checkbox" '+(conv.enabled?'checked':'')+'> habilitada</label></div><div class="fields" style="margin-top:12px"><div class="field"><label>Números autorizados · uno por línea</label><textarea id="convNumbers" placeholder="5493532...">'+esc((conv.authorizedNumbers||[]).join('\n'))+'</textarea></div><div><div class="field"><label>Máx. mensajes de contexto</label><input id="convMax" type="number" min="10" max="500" value="'+Number(conv.maxContextMessages||80)+'"></div><div class="small muted" style="margin-top:8px">Usá código de país. Nexo normaliza +, espacios y guiones. Revocar un número bloquea respuestas inmediatamente.</div><button id="checkReplies" style="margin-top:12px">Ver respuestas pendientes</button></div></div><div id="convst" class="status small"></div><hr style="border:0;border-top:1px solid var(--line);margin:18px 0"><div class="row"><div><strong>LLM para barrido y resumen</strong><div class="small muted">OpenAI-compatible · sólo analiza datos; nunca autoriza envíos.</div></div><label class="check"><input id="llmEnabled" type="checkbox" '+(llm.enabled?'checked':'')+'> habilitado</label></div><div class="fields" style="margin-top:12px"><div class="field"><label>Base URL</label><input id="llmUrl" value="'+esc(llm.baseUrl)+'"></div><div class="field"><label>Modelo</label><input id="llmModel" value="'+esc(llm.model)+'"></div><div class="field"><label>Temperatura</label><input id="llmTemp" type="number" step="0.1" min="0" max="2" value="'+Number(llm.temperature)+'"></div><div class="field"><label>Máx. mensajes por barrido</label><input id="llmMax" type="number" min="20" max="5000" value="'+Number(llm.maxInputMessages)+'"></div><div class="field"><label>API key</label><input id="llmKey" type="password" placeholder="'+(s.llmApiKeyConfigured?'Configurada · dejar vacío para conservar':'Opcional para endpoints locales')+'"></div><div class="field"><label>Estado secreto</label><input disabled value="'+(s.llmApiKeyConfigured?'API key configurada':'Sin API key')+'"></div></div><div class="field" style="margin-top:10px"><label>System prompt</label><textarea id="llmPrompt">'+esc(llm.systemPrompt)+'</textarea></div><div class="cluster" style="margin-top:12px"><button id="saveCfg" class="primary">Guardar configuración</button><button id="testLlm">Probar resumen</button></div><div id="cfgst" class="status small"></div></section>'}
function render(state,settings){const accounts=state.accounts||[],hasOutput=accounts.some(a=>a.role==='output');app.innerHTML='<div class="grid"><section class="card wide"><div class="row"><div><strong>Agregar cuenta</strong><div class="small muted">Podés crear todos los inputs que quieras; output está limitado a uno. Nexo inicia automáticamente la vinculación.</div></div></div><form id="add" class="fields3" style="margin-top:12px"><input name="label" placeholder="Ej: WhatsApp Diego" required maxlength="120"><select name="role"><option value="input">Input</option><option value="output" '+(hasOutput?'disabled':'')+'>Output de Codex</option></select><button class="primary">Crear y vincular</button></form><div id="addst" class="status small"></div></section>'+accounts.map(accountCard).join('')+'<section class="card wide"><strong>Buscar en inputs</strong><div class="cluster" style="margin-top:12px"><input id="q" placeholder="Ej: Juan bomba" style="max-width:420px"><button id="search">Buscar</button><button id="recent">Recientes</button></div><div id="results" class="list"></div></section>'+(hasOutput?'<section class="card wide"><strong>Probar output</strong><p class="small muted">Diagnóstico local; el MCP usa el mismo canal.</p><div class="fields3"><input id="to" placeholder="549... o JID"><input id="msg" placeholder="Mensaje"><button id="send">Enviar</button></div><div id="sendst" class="status small"></div></section>':'')+settingsCard(settings)+'</div>';bind()}
function showMessages(messages){const slot=document.getElementById('results');slot.innerHTML=(messages||[]).length?(messages||[]).map(m=>'<div class="message"><div class="small muted">'+esc(new Date(m.occurredAt).toLocaleString())+' · '+esc(m.accountLabel)+' · '+esc(m.chatName||m.chatJid)+(m.senderName?' · '+esc(m.senderName):'')+'</div><div class="text">'+esc(m.text||'['+(m.messageType||'mensaje sin texto')+']')+'</div></div>').join(''):'<div class="muted small">Sin resultados.</div>'}
function bind(){document.getElementById('add').onsubmit=async e=>{e.preventDefault();const fd=new FormData(e.currentTarget),st=document.getElementById('addst');st.textContent='Creando cuenta…';const out=await api('/api/accounts',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({label:fd.get('label'),role:fd.get('role')})});if(!out.r.ok){st.className='status small error';st.textContent=out.b.error||'Error';return}st.textContent='Iniciando vínculo…';await api('/api/accounts/'+encodeURIComponent(out.b.id)+'/connect',{method:'POST'});await load()};document.querySelectorAll('[data-connect]').forEach(b=>b.onclick=async()=>{b.disabled=true;const card=b.closest('.card'),open=card.querySelector('.badge')?.textContent?.includes('open');const out=await api('/api/accounts/'+encodeURIComponent(b.dataset.connect)+'/'+(open?'restart':'connect'),{method:'POST'});if(!out.r.ok)alert(out.b.error||'Error');await load()});document.querySelectorAll('[data-delete]').forEach(b=>b.onclick=async()=>{if(!confirm('Esto desvincula WhatsApp y elimina los datos de esta cuenta. ¿Continuar?'))return;const out=await api('/api/accounts/'+encodeURIComponent(b.dataset.delete)+'/logout',{method:'POST'});if(!out.r.ok)alert(out.b.error||'Error');await load()});document.getElementById('search').onclick=async()=>{const q=document.getElementById('q').value.trim();if(q.length<2)return;const out=await api('/api/messages/search',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({query:q,limit:60})});showMessages(out.b.messages)};document.getElementById('recent').onclick=async()=>{const out=await api('/api/messages/recent?limit=40');showMessages(out.b.messages)};const send=document.getElementById('send');if(send)send.onclick=async()=>{const st=document.getElementById('sendst');st.textContent='Enviando…';const out=await api('/api/output/send',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({to:document.getElementById('to').value,text:document.getElementById('msg').value,reason:'manual admin test',confirmedByUser:true})});st.className='status small '+(out.r.ok?'good':'error');st.textContent=out.r.ok?'Enviado · '+(out.b.audit?.messageId||'ok'):(out.b.error||'Error')};document.getElementById('saveCfg').onclick=async()=>{const st=document.getElementById('cfgst');st.textContent='Guardando…';const key=document.getElementById('llmKey').value;const numbers=document.getElementById('convNumbers').value.split(/[\n,;]+/).map(v=>v.trim()).filter(Boolean);const body={autoConnectLinkedAccounts:document.getElementById('cfgAuto').checked,openDashboardOnLaunch:document.getElementById('cfgOpen').checked,uiRefreshMs:Number(document.getElementById('cfgRefresh').value),maxSearchResults:Number(document.getElementById('cfgMax').value),windowsAutostart:document.getElementById('cfgWin').checked,outputConversation:{enabled:document.getElementById('convEnabled').checked,authorizedNumbers:numbers,maxContextMessages:Number(document.getElementById('convMax').value)},llm:{enabled:document.getElementById('llmEnabled').checked,baseUrl:document.getElementById('llmUrl').value,model:document.getElementById('llmModel').value,temperature:Number(document.getElementById('llmTemp').value),maxInputMessages:Number(document.getElementById('llmMax').value),systemPrompt:document.getElementById('llmPrompt').value},...(key?{llmApiKey:key}:{})};const out=await api('/api/settings',{method:'PUT',headers:{'content-type':'application/json'},body:JSON.stringify(body)});st.className='status small '+(out.r.ok?'good':'error');st.textContent=out.r.ok?'Configuración guardada.':(out.b.error||'Error')};document.getElementById('checkReplies').onclick=async()=>{const st=document.getElementById('convst');st.textContent='Consultando…';const out=await api('/api/output/conversation/replies?pending=true&limit=20');st.className='status small '+(out.r.ok?'good':'error');st.textContent=out.r.ok?((out.b.messages||[]).length+' respuesta(s) pendiente(s).'):(out.b.error||'Error')};document.getElementById('testLlm').onclick=async()=>{const st=document.getElementById('cfgst');st.textContent='Resumiendo últimos mensajes…';const out=await api('/api/llm/summarize',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({limit:80,focus:'Prueba de configuración: resumen breve de actividad reciente'})});st.className='status small '+(out.r.ok?'good':'error');st.textContent=out.r.ok?out.b.summary:(out.b.error||'Error')}}
async function load(){const [state,settings]=await Promise.all([api('/api/state'),api('/api/settings')]);if(!state.r.ok||!settings.r.ok){app.innerHTML='<div class="card wide error">'+esc(state.b.error||settings.b.error||'No se pudo cargar')+'</div>';return}render(state.b,settings.b);const waiting=(state.b.accounts||[]).some(a=>['connecting','qr','reconnecting'].includes(a.runtime?.state));if(waiting)setTimeout(load,Number(state.b.settings?.uiRefreshMs||1500))}
load();
</script></body></html>`;
}
